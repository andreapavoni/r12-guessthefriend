<h1>Leaderboard</h1>

<table>
  <th>
    <td>Name</td>
    <td>Points</td>
  </th>
  <% @players.each do |player| %>
  <tr>
    <td><%= player.name %></td>
    <td><%= player.score %></td>
  </tr>
<% end %>
</table>
