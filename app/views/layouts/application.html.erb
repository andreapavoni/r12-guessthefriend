<!DOCTYPE html>
<html>
<head>
  <title>Guesswho</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= google_analytics %>
</head>
<body>

    <div id="user_nav">
      <% if current_user %>
        Signed in as <strong><%= current_user.name %></strong>!
        <%= link_to "Sign out", signout_path, id: "sign_out" %>
      <% else %>
        <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
      <% end %>
    </div>

    <!-- Place this tag everywhere you want a +1 button -->
    <g:plusone></g:plusone>
    
  <%= yield %>

    <%# + GOOGLE +1 BUTTON + %>
    <%# For the best performance, put this code immediately after the last +1 tag. %>
    <%= google_plusone %>
    <%# = GOOGLE +1 BUTTON = %>

  </body>
</html>
